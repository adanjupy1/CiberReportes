# Backend Agente Bit — Dependencias
# Instalar con: pip install -r requirements.txt

# ── Core ──────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# ── Modelo GGUF local ─────────────────────────────────
llama-cpp-python>=0.3.0

# ── RAG: base vectorial + embeddings ─────────────────
chromadb>=1.0.0
sentence-transformers>=3.0.0
